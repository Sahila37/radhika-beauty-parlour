// ===========================================================
// EDIT THIS after you deploy the backend (see README.md).
// While testing locally with `npm run dev` in /backend, keep it as-is.
// After deploying (e.g. to Render), replace with your live URL, e.g.:
// const API_BASE_URL = "https://radhika-backend.onrender.com";
// ===========================================================
const API_BASE_URL = "http://localhost:5000";
